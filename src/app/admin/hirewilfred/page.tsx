import HireWilfredFrame from '@/components/HireWilfredFrame';

export const metadata = { title: 'HireWilfred — Ops Console' };

export default function HireWilfredOpsPage() {
    return (
        <HireWilfredFrame
            src="/hirewilfred/ops.html"
            title="Ops Console"
            blurb="Wilfred opportunity desk, delivery pipeline, scenario quoting and the cost model behind every quote. A lead becomes a prospective client at Booked, and gets sized and priced here after the first meeting."
        />
    );
}
